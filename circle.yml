general:
  branches:
    only:
      - master
      - develop
deployment:
  production:
    branch: master
    commands:
      - ./deploy-to-heroku-master
deployment:
  staging:
    branch: develop
    commands:
      - ./deploy-to-heroku-develop
